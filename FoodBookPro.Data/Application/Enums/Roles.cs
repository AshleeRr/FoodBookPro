namespace FoodBookPro.Data.Application.Enums
{
    public enum Roles//Comment Enum: Represents different user roles in the application
    {
        CLIENT = 1, // Regular user of the application
        OWNER = 2, // Restaurant owner or manager
        ADMIN = 3 // Administrator with full access to the system
    }
}
